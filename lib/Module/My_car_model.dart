import 'package:flutter/material.dart';

class my_car_model extends StatelessWidget {
  const my_car_model({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
